{
  "eval_id": 1,
  "eval_name": "external-whisper-api-error",
  "prompt": "Getting this error on the call reviews upload route: 'TypeError: Cannot read properties of undefined (reading 'text')'. It's happening when the Whisper transcription comes back — seems like the response shape changed or something. Here's the error from the logs: 'at processTranscription (src/app/api/call-reviews/upload/route.ts:87:34)'",
  "assertions": [
    "Announces the /debug pipeline at the start (contains 'Reproduce' and 'Classify' in the opening)",
    "Reads the actual file src/app/api/call-reviews/upload/route.ts before proposing any fix",
    "Classifies the bug as EXTERNAL (not INTERNAL or ENVIRONMENT) with explicit reasoning",
    "Uses a research tool (Perplexity, web search, or similar) to check OpenAI Whisper API response shape before proposing a fix",
    "Does NOT propose a code change before completing the research step"
  ]
}
