{
  "eval_id": 2,
  "eval_name": "internal-prospect-name-missing",
  "prompt": "The call review list is showing 'Unknown' for prospect_name on every row, even for calls where I can see the name in the database. This just started happening — I didn't change anything obvious. Can you figure out what's broken?",
  "assertions": [
    "Classifies the bug as INTERNAL (data/logic bug within src/) with explicit reasoning",
    "Does NOT fire a Perplexity or external research search for this internally-scoped bug",
    "Reads at least one relevant call review component or API route to trace the data flow",
    "Checks git log or recent changes on relevant files to find what changed",
    "States a root cause hypothesis before proposing a fix"
  ]
}
